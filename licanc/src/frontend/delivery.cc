#include "frontend/delivery.hh"

frontend::delivery::t_compilation_unit frontend::delivery::deliver_that_shit(const frontend::delivery::t_delivery_config& config) {
    t_compilation_unit compilation_unit;

    // recursively search for all files in the given folder

    // go through each individual file and run them through the frontend process. give each individual file
    // its own t_compilation_file.

    return compilation_unit;
}